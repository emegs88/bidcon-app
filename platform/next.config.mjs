/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Projeto Vercel SEPARADO do site estático (futuro app.bidcon.com.br).
  // O site de marketing (bidcon.com.br) continua intocado em /public.
};

export default nextConfig;
